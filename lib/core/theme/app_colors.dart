import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const Color background = Color(0xFF101418);
  static const Color surface = Color(0xFF1E1E1E);
  static const Color cardBackground = Color(0xFF25282B);
  static const Color inputBackground = Color(0xFF8B938D);

  // Brand Colors
  static const Color primary = Color(0xFF1B4332); // Deep Green
  static const Color primaryLight = Color(0xFF74C69D); // Mint Accent
  static const Color secondary = Color(0xFF2D2F31); // Dark Neutral

  // Status Colors
  static const Color error = Color(0xFFD00000); // Tertiary Red
  static const Color success = Color(0xFF40916C);

  // Text Colors
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFB0B0B0);
  static const Color textMuted = Color(0xFF707070);

  // Accents
  static const Color divider = Color(0xFF333333);
  static const Color highlight = Color(0xFF2D2F31);
}
